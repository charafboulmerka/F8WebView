package com.sham.chataraby
import android.app.Application

/*
import com.onesignal.OSNotificationOpenResult
import com.onesignal.OneSignal




class MyNotificationOpenedHandler(private val application: Application) :
    OneSignal.NotificationOpenedHandler {

    override fun notificationOpened(result: OSNotificationOpenResult) {
        /*

        // Get custom datas from notification
        val data = result.notification.payload.launchURL
        if (data != null) {

            Log.e("FIRST_OPEN",data.toString())
            val intent = Intent(application, NavigationActivity::class.java)
                .setFlags(Intent.FLAG_ACTIVITY_REORDER_TO_FRONT or Intent.FLAG_ACTIVITY_NEW_TASK or Intent.FLAG_ACTIVITY_CLEAR_TOP)
                .putExtra("LINK",data)
            application.startActivity(intent)
        }
        else{
            Log.e("FIRST_OPEN","IS NULL")
            val intent = Intent(application, SplashScreen::class.java)
                .setFlags(Intent.FLAG_ACTIVITY_REORDER_TO_FRONT or Intent.FLAG_ACTIVITY_NEW_TASK)
            application.startActivity(intent)
        }

        // React to button pressed
       // val actionType = result.action.type
      //  if (actionType == OSNotificationAction.ActionType.ActionTaken)
        //    Log.i("OneSignalExample", "Button pressed with id: " + result.action.actionID)

        // Launch new activity using Application object
        //startApp()

         */
    }




}

 */
